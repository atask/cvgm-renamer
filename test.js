const tap = require('tap')
// const sinon = require('sinon')
// const proxyquire = require('proxyquire')

// tap.runOnly = true

tap.test('test is empty', t => {
  t.ok(false, 'No tests provided for this module')
  t.end()
})
